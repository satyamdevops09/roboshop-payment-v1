
name: CICD - Main
on:
 
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  reuse:
    uses: satyamdevops09/roboshop-github-actions/.github/workflows/ci-main.yml@main
    with:
      app_type: python
      app_name: payment